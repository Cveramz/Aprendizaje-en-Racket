#lang racket
; Al definir caracteres se agregan comillas
(define nombre "eduardo")
;Al definir un numero se hace así
(define edad 20)
#|
Todo esto es un comentario de muchas lineas
:D
|#

;f()Media poner dos numeros y dividirlos en dos.

(define (media a b) (/(+ a b) 2))
;en (media a b) son los argumentos - lo de la derecha es la salida
